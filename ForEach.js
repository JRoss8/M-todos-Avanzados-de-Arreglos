function enviarCorreoConfirmacion(asistentes) {
  asistentes.forEach(asistente => {
mensaje)
    console.log(`Enviando correo de confirmación a: ${asistente}`);
    
    console.log(`El correo de confirmación ha sido enviado a: ${asistente}`);
  });
}

const asistentes = ["Ana", "Luis", "Carlos", "Marta"];

enviarCorreoConfirmacion(asistentes);
